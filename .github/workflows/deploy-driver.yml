# DISABLED: Driver app has been merged into the POS app (apps/pos).
# Role-based routing is now handled by the unified POS app via deploy-pos.yml.
# Drivers are routed to /driver/dashboard after login.
# See apps/pos/src/pages/driver/ for the driver UI pages.
#
# This workflow is intentionally a no-op.

name: Deploy Driver App (DISABLED — merged into POS)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Driver app merged into POS
        run: echo "Driver app has been merged into the POS app. See deploy-pos.yml for the unified deployment."
